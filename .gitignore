tags
test.sh
.luarc.json
nvim

spell/

# Personal fork: track nvim-pack-lock.json in version control
# - see :help vim.pack-lockfile
# (Upstream ignores it to avoid unneeded merge conflicts.)
# nvim-pack-lock.json

.DS_Store
